export class Authority {
    private authorityId: string
    private authority: string

    constructor(private authorityy: string, private authorityyId: string){
        this.authority = authorityy;
        this.authorityId = authorityyId;
    }
}
